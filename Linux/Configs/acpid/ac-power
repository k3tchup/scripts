# /etc/acpi/events/ac-power
# This is called when an AC adapter is plugged in
# /etc/acpi/ac-power.sh for further processing.

# Optionally you can specify the placeholder %e. It will pass
# through the whole kernel event message to the program you've
# specified.

event=ac_adapter.*
action=/etc/acpi/ac-power.sh %e
